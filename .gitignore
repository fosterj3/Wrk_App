# OneDrive / Windows cruft
desktop.ini
Thumbs.db
*.tmp

# Local Claude Code settings
.claude/settings.local.json

# Backup files exported by the app during testing
wrk-backup-*.json
cadence-backup-*.json
cadence-workouts-*.csv
# The supplied brand sheet. Kept out of the repo (it is artwork, not source);
# tools/make-icons.html reads it to regenerate the icons.
brand-source.png
